{
  "name": "uniswap-v3-lab",
  "version": "1.0.0",
  "description": "Uniswap V3 core math model in TypeScript - exact match with on-chain contract behavior",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "cli": "bun run cli/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "puppeteer": "^24.40.0"
  }
}
